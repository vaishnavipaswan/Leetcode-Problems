# Write your MySQL query statement below
SELECT U.NAME,SUM(CASE WHEN R.DISTANCE IS NULL THEN 0 ELSE R.DISTANCE END) AS TRAVELLED_DISTANCE FROM USERS U LEFT JOIN RIDES R ON U.ID=R.USER_ID GROUP BY U.ID,U.NAME ORDER BY TRAVELLED_DISTANCE DESC,NAME ASC;