# Write your MySQL query statement below
SELECT DISTINCT C.CUSTOMER_ID FROM CUSTOMER C GROUP BY CUSTOMER_ID HAVING COUNT(DISTINCT C.PRODUCT_KEY)=(SELECT COUNT(DISTINCT PRODUCT_KEY) FROM PRODUCT ) ORDER BY CUSTOMER_ID;